"use strict"; // treat all JS code as newer version

//alert (3+3) // We are using nodejs and not browser so it won't work

// number
// bigint
// string => ""
// boolean => true/false

// null => standalone value
// undefined => means value has not been assigned yet
// symbol => figma tools use this , to define unique components

// object

// null is of type object
// undefined is of type undefined

console.log(typeof null) // output will object
console.log(typeof undefined) // output will be undefined 